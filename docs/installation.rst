============
Installation
============

At the command line either::

    $ easy_install librator
    $ pip install librator

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv librator
    $ pip install librator
